(() => {
  'use strict';
  const A = window.RachnaAPI;
  if (!A) return;
  const $ = (s,r=document) => r.querySelector(s);
  const esc = A.esc || (s => String(s ?? '').replace(/[&<>\"']/g, c => ({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c])));
  const money = A.money || (n => 'NPR ' + Number(n||0).toLocaleString('en-IN',{maximumFractionDigits:0}));
  const stages = [['new','New enquiry'],['quote_pending','Quotation pending'],['quote_made','Quotation made'],['quote_sent','Quotation sent'],['interested','Interested'],['awaiting_advance','Advance pending'],['booked','Booked'],['not_interested','Not interested'],['lost','Lost'],['passed_on','Passed on'],['booked_elsewhere','Booked elsewhere'],['cancelled','Cancelled'],['booking_cancelled','Booking cancelled']];
  const sourceList = ['Facebook','Instagram','TikTok','Google','WhatsApp','Referral','Walk-in','Website','Other'];
  const state = { search:'', source:'all', stage:'all' };
  const toast = (m,error=false)=>{const n=$('#toast');if(!n)return;n.textContent=m;n.className='toast show'+(error?' error':'');clearTimeout(window.__crmToast);window.__crmToast=setTimeout(()=>n.className='toast',3000)};
  const statusLabel = v => stages.find(x=>x[0]===v)?.[1] || v || '—';
  const customer = id => A.state.customers.find(c=>c.id===id);
  const converted = i => A.state.projects.find(p=>p.inquiry_id===i.id);
  const css=document.createElement('style');
  css.textContent=`
    .crm-wrap{display:grid;gap:18px}.crm-kpis{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:12px}.crm-kpi{background:#fff;border:1px solid #e1ddd5;border-radius:15px;padding:15px}.crm-kpi span{font-size:11px;text-transform:uppercase;letter-spacing:.07em;color:#817b73}.crm-kpi strong{display:block;font-size:23px;margin-top:5px}.crm-kpi em{font-size:12px;font-style:normal;color:#8a847c}.crm-toolbar{display:flex;justify-content:space-between;gap:12px;align-items:center;flex-wrap:wrap}.crm-filters{display:flex;gap:8px;flex-wrap:wrap}.crm-search{min-width:280px}.crm-table{width:100%;border-collapse:collapse}.crm-table th,.crm-table td{padding:11px 12px;border-bottom:1px solid #eee9e2;text-align:left;font-size:13px;vertical-align:top}.crm-table th{font-size:10px;text-transform:uppercase;letter-spacing:.06em;color:#817b73;background:#faf9f7}.crm-table select{width:150px}.crm-client b,.crm-client small{display:block}.crm-client small{color:#8b857d;margin-top:3px}.crm-source{display:inline-flex;padding:4px 8px;border-radius:999px;background:#f1eee8;color:#625d56;font-size:11px}.crm-conv{font-size:11px;color:#26734d}.crm-loss{color:#9a4037}.crm-cards{display:grid;grid-template-columns:1fr 1fr;gap:14px}.crm-card{background:#fff;border:1px solid #e1ddd5;border-radius:15px;padding:16px}.crm-card h3{margin:0 0 4px}.crm-card p{margin:0;color:#817b73;font-size:12px}.crm-list{display:grid;gap:8px;margin-top:12px}.crm-line{display:flex;justify-content:space-between;gap:12px;padding:8px 0;border-bottom:1px dashed #e7e2da}.crm-line:last-child{border-bottom:0}.crm-bar{height:8px;background:#eee9e2;border-radius:999px;overflow:hidden}.crm-bar i{display:block;height:100%;background:#332f2a}.crm-value{display:grid;gap:5px}.crm-empty{padding:22px;text-align:center;color:#7b756d}.crm-modal-grid{display:grid;grid-template-columns:1fr 1fr;gap:12px}.crm-full{grid-column:1/-1}.crm-note{font-size:12px;color:#7d776f;margin-top:9px}.crm-action-row{display:flex;gap:8px;flex-wrap:wrap}.crm-muted{color:#8a847c;font-size:12px}@media(max-width:1100px){.crm-kpis{grid-template-columns:repeat(3,minmax(0,1fr))}}@media(max-width:700px){.crm-kpis{grid-template-columns:1fr 1fr}.crm-cards{grid-template-columns:1fr}.crm-modal-grid{grid-template-columns:1fr}.crm-full{grid-column:auto}.crm-table{min-width:1050px}.crm-search{min-width:200px}}
  `;document.head.appendChild(css);
  function modal(title,body,actions=''){const n=$('#modal');if(!n)return;n.innerHTML=`<div class="modal-head"><div><div class="eyebrow">CRM & SALES</div><h2>${esc(title)}</h2></div><button class="close-btn" data-crm-action="close">×</button></div><div class="modal-body">${body}</div><div class="modal-foot"><button class="btn" data-crm-action="close">Cancel</button>${actions}</div>`;$('#backdrop')?.classList.add('show')}
  function page(){const rows=A.state.inquiries.filter(i=>{const c=customer(i.customer_id);const hay=[i.event_name,i.source,i.status,i.event_date_bs,i.venue,c?.name,c?.phone].join(' ').toLowerCase();return(!state.search||hay.includes(state.search.toLowerCase()))&&(state.source==='all'||i.source===state.source)&&(state.stage==='all'||i.status===state.stage)});const all=A.state.inquiries;const bookings=all.filter(i=>i.status==='booked'||converted(i));const quoted=all.filter(i=>converted(i));const open=all.filter(i=>!['lost','booked','cancelled','booking_cancelled','booked_elsewhere','not_interested','passed_on'].includes(i.status));const totalBudget=all.reduce((s,i)=>s+Number(i.budget||0),0);const sourceMap={};all.forEach(i=>sourceMap[i.source||'Other']=(sourceMap[i.source||'Other']||0)+1);const max=Math.max(1,...Object.values(sourceMap));const stageMap={};all.forEach(i=>stageMap[i.status||'new']=(stageMap[i.status||'new']||0)+1);return `<div class="crm-wrap"><div class="page-head"><div><div class="eyebrow">CRM & SALES</div><h1>Lead & Conversion Center</h1><p>Track every enquiry from first contact to Event ID, booking and revenue.</p></div><div class="actions"><button class="btn primary" data-action="new-inquiry">＋ New enquiry</button></div></div><div class="crm-kpis"><div class="crm-kpi"><span>Total enquiries</span><strong>${all.length}</strong><em>all captured leads</em></div><div class="crm-kpi"><span>Open pipeline</span><strong>${open.length}</strong><em>needs follow-up</em></div><div class="crm-kpi"><span>Converted</span><strong>${quoted.length}</strong><em>linked to Event ID</em></div><div class="crm-kpi"><span>Booked</span><strong>${bookings.length}</strong><em>won business</em></div><div class="crm-kpi"><span>Lead budget</span><strong>${money(totalBudget)}</strong><em>captured budget</em></div></div><div class="crm-toolbar"><div class="crm-filters"><input class="search crm-search" id="crmSearch" value="${esc(state.search)}" placeholder="Search client, event, phone, source"><select class="search" id="crmSource"><option value="all">All sources</option>${[...new Set([...sourceList,...Object.keys(sourceMap)])].sort().map(s=>`<option value="${esc(s)}" ${s===state.source?'selected':''}>${esc(s)}</option>`).join('')}</select><select class="search" id="crmStage"><option value="all">All stages</option>${stages.map(s=>`<option value="${s[0]}" ${s[0]===state.stage?'selected':''}>${esc(s[1])}</option>`).join('')}</select></div><div class="crm-muted">Booking conversion: ${all.length?(bookings.length/all.length*100).toFixed(1):'0.0'}%</div></div><section class="panel table-panel"><div class="panel-title"><div><div class="eyebrow">LEAD REGISTER</div><h2>Every enquiry</h2><p>Change the stage here; conversion keeps the same customer and creates one Event ID.</p></div></div><div style="overflow:auto"><table class="crm-table"><thead><tr><th>Client / Event</th><th>Source</th><th>Date / Venue</th><th>Budget</th><th>Stage</th><th>Event ID</th><th></th></tr></thead><tbody>${rows.length?rows.map(i=>{const c=customer(i.customer_id),p=converted(i);return `<tr><td class="crm-client"><b>${esc(c?.name||i.customer_name||'Unknown')}</b><small>${esc(i.event_name||'Event')} · ${esc(c?.phone||i.phone||'')}</small></td><td><span class="crm-source">${esc(i.source||'Other')}</span></td><td>${esc(i.event_date_bs||'Date TBC')}<small class="crm-muted">${esc(i.venue||'Venue TBC')}</small></td><td>${i.budget?money(i.budget):'—'}</td><td><select data-crm-stage="${esc(i.id)}">${stages.map(s=>`<option value="${s[0]}" ${s[0]===i.status?'selected':''}>${esc(s[1])}</option>`).join('')}</select></td><td>${p?`<b>${esc(p.event_code||'Event')}</b>`:'<span class="crm-muted">Not converted</span>'}</td><td class="crm-action-row"><button class="btn tiny" data-crm-open="${esc(i.id)}">Open</button>${!p&&['lost','booked','cancelled','booking_cancelled','booked_elsewhere','not_interested','passed_on'].includes(i.status)===false?`<button class="btn tiny" data-crm-convert="${esc(i.id)}">Convert</button>`:''}</td></tr>`}).join(''):`<tr><td colspan="7"><div class="crm-empty">No enquiries match these filters.</div></td></tr>`}</tbody></table></div></section><div class="crm-cards"><section class="crm-card"><h3>Lead sources</h3><p>Which channels are creating your pipeline?</p><div class="crm-list">${Object.entries(sourceMap).sort((a,b)=>b[1]-a[1]).map(([s,n])=>`<div class="crm-line"><span>${esc(s)}</span><b>${n}</b></div>`).join('')||'<div class="crm-empty">No source data yet.</div>'}</div></section><section class="crm-card"><h3>Pipeline stages</h3><p>Where leads are currently sitting.</p><div class="crm-list">${Object.entries(stageMap).sort((a,b)=>b[1]-a[1]).map(([s,n])=>`<div class="crm-value"><div class="crm-line"><span>${esc(statusLabel(s))}</span><b>${n}</b></div><div class="crm-bar"><i style="width:${Math.round(n/max*100)}%"></i></div></div>`).join('')}</div></section></div></div>`}
  function openLead(id){const i=A.state.inquiries.find(x=>x.id===id);if(!i)return;const c=customer(i.customer_id),p=converted(i);modal('Lead detail',`<div class="crm-modal-grid"><label class="field"><span>Client</span><input value="${esc(c?.name||i.customer_name||'')}" disabled></label><label class="field"><span>Phone</span><input value="${esc(c?.phone||i.phone||'')}" disabled></label><label class="field"><span>Event</span><input id="crmEvent" value="${esc(i.event_name||'')}"></label><label class="field"><span>Source</span><select id="crmSourceEdit">${[...new Set([...sourceList,i.source||'Other'])].map(s=>`<option value="${esc(s)}" ${s===(i.source||'Other')?'selected':''}>${esc(s)}</option>`).join('')}</select></label><label class="field"><span>Status</span><select id="crmStatusEdit">${stages.map(s=>`<option value="${s[0]}" ${s[0]===i.status?'selected':''}>${esc(s[1])}</option>`).join('')}</select></label><label class="field"><span>Budget</span><input id="crmBudget" type="number" min="0" value="${Number(i.budget||0)}"></label><label class="field crm-full"><span>Notes</span><textarea id="crmNotes">${esc(i.notes||'')}</textarea></label></div><p class="crm-note">${p?`Converted to <b>${esc(p.event_code||'Event')}</b>.`:'Not converted yet. Converting creates the same Event ID used by operations, crew, vendors and finance.'}</p>`,`${p?'':`<button class="btn soft" data-crm-action="convert" data-id="${esc(id)}">Convert to Event ID</button>`}<button class="btn primary" data-crm-action="save" data-id="${esc(id)}">Save changes</button>`)}
  document.addEventListener('click',e=>{const t=e.target.closest('[data-crm-action]');if(t){e.preventDefault();e.stopImmediatePropagation();(async()=>{try{const a=t.dataset.crmAction;if(a==='close')return $('#backdrop')?.classList.remove('show');if(a==='open-events'){return;}if(a==='save'){await A.updateInquiry(t.dataset.id,{event_name:$('#crmEvent').value.trim(),source:$('#crmSourceEdit').value,status:$('#crmStatusEdit').value,budget:Number($('#crmBudget').value||0),notes:$('#crmNotes').value||null,updated_at:new Date().toISOString()});$('#backdrop')?.classList.remove('show');toast('Lead updated');render();}if(a==='convert'){const p=await A.convertInquiry(t.dataset.id);$('#backdrop')?.classList.remove('show');toast(`Converted · ${p.event_code||'Event ID created'}`);render();}}catch(err){toast(err?.message||'Could not update lead',true)}})()}}},true);
  document.addEventListener('click',e=>{const o=e.target.closest('[data-crm-open]');if(o){e.preventDefault();e.stopImmediatePropagation();openLead(o.dataset.crmOpen)}const c=e.target.closest('[data-crm-convert]');if(c){e.preventDefault();e.stopImmediatePropagation();(async()=>{try{const p=await A.convertInquiry(c.dataset.crmConvert);toast(`Converted · ${p.event_code||'Event ID created'}`);render()}catch(err){toast(err?.message||'Could not convert lead',true)}})()}}},true);
  document.addEventListener('change',e=>{const s=e.target.closest('[data-crm-stage]');if(s){(async()=>{try{await A.updateInquiry(s.dataset.crmStage,{status:s.value,updated_at:new Date().toISOString()});toast('Stage updated');render()}catch(err){toast(err?.message||'Could not update stage',true)}})()}if(e.target.id==='crmSource') {state.source=e.target.value;render()}if(e.target.id==='crmStage'){state.stage=e.target.value;render()}},true);
  document.addEventListener('input',e=>{if(e.target.id==='crmSearch'){state.search=e.target.value;render()}},true);
  document.addEventListener('click',e=>{const route=e.target.closest('[data-route="inquiries"]');if(route){e.preventDefault();e.stopImmediatePropagation();state.search='';state.source='all';state.stage='all';render()}},true);
  const oldAction=window.__crmNavPatched;if(!oldAction)window.__crmNavPatched=true;
  function render(){page() && ($('#page').innerHTML=page());}
  const navButtons=document.getElementById('nav');
  const obs=new MutationObserver(()=>{const nav=document.getElementById('nav');if(nav&&!nav.querySelector('[data-route="inquiries"]')){const group=document.createElement('div');group.className='nav-group';group.innerHTML='<div class="nav-label">Sales</div><button class="nav-item" data-route="inquiries"><span class="nav-ico">✉</span><span>CRM & Conversion</span></button>';nav.prepend(group)}});if(navButtons)obs.observe(navButtons,{childList:true,subtree:true});
})();
