# Rachna OS

Production architecture:
- `index.html` — application shell
- `app.js` — single frontend application
- `styles.css` — visual system
- `supabase/schema.sql` — database source

Legacy patch files are not part of production.

Date picker: external Nepali date picker and locked.

Finance transactions: customer payments and expenses support edit/delete controls.