(()=>{
'use strict';
if(window.__RACHNA_GLOBAL_BS_V1__)return;window.__RACHNA_GLOBAL_BS_V1__=true;
const DC=window.RachnaDateCheck;if(!DC?.bsToAd||!DC?.adToBs)return;
const MONTHS=['Baisakh','Jestha','Ashadh','Shrawan','Bhadra','Ashwin','Kartik','Mangsir','Poush','Magh','Falgun','Chaitra'];
const FIRST=2000,LAST=2099,pad=n=>String(n).padStart(2,'0'),esc=s=>String(s??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[c]));
function parts(v){const q=String(v||'').match(/^(\d{4})-(\d{1,2})-(\d{1,2})$/);return q?{y:+q[1],m:+q[2],d:+q[3]}:null}
function days(y,m){for(let d=32;d>=28;d--)if(DC.bsToAd(`${y}-${pad(m)}-${pad(d)}`))return d;return 30}
function today(){const ps=new Intl.DateTimeFormat('en-CA',{timeZone:'Asia/Kathmandu',year:'numeric',month:'2-digit',day:'2-digit'}).formatToParts(new Date()),o={};ps.forEach(p=>{if(p.type!=='literal')o[p.type]=p.value});return `${o.year}-${o.month}-${o.day}`}
function todayBs(){const p=DC.adToBs(today());return p?`${p.y}-${pad(p.m)}-${pad(p.d)}`:''}
function picker(p){const md=days(p.y,p.m),dd=Math.min(p.d||1,md);return `<div class="os-bs-picker"><select data-bs-y aria-label="BS year">${Array.from({length:LAST-FIRST+1},(_,i)=>FIRST+i).map(y=>`<option value="${y}" ${y===p.y?'selected':''}>${y}</option>`).join('')}</select><select data-bs-m aria-label="BS month">${MONTHS.map((m,i)=>`<option value="${i+1}" ${i+1===p.m?'selected':''}>${m}</option>`).join('')}</select><select data-bs-d aria-label="BS day">${Array.from({length:md},(_,i)=>i+1).map(d=>`<option value="${d}" ${d===dd?'selected':''}>${d}</option>`).join('')}</select></div>`}
function style(){if(document.getElementById('os-bs-style'))return;const s=document.createElement('style');s.id='os-bs-style';s.textContent='.os-bs-picker{display:grid;grid-template-columns:1.05fr 1fr .75fr;gap:8px;margin-top:4px}.os-bs-picker select{width:100%;min-height:40px;padding:0 9px;border-radius:9px}.os-bs-note{display:block;margin-top:4px;font-size:11px;opacity:.65}.os-bs-hidden{position:absolute!important;width:1px!important;height:1px!important;opacity:0!important;pointer-events:none!important}.os-bs-source{display:none!important}';document.head.appendChild(s)}
function changeLabel(input){const label=input.closest('label');const sp=label?.querySelector('span');if(!sp)return;let t=sp.textContent||'';if(/date/i.test(t))t=t.replace(/\s*\(AD\)/ig,'').replace(/\s*\(BS\)/ig,'')+' (BS)';sp.textContent=t}
function wrapNative(input){if(input.dataset.osBsWrapped==='1'||input.type!=='date')return;style();changeLabel(input);const ad=input.value||'';const bs=parts(DC.adToBs(ad));const fallback=parts(todayBs())||{y:2083,m:1,d:1};const p=bs||fallback;input.dataset.osBsWrapped='1';input.classList.add('os-bs-hidden');const box=document.createElement('div');box.className='os-bs-control';box.innerHTML=picker(p)+'<span class="os-bs-note">Nepali date · BS</span>';input.insertAdjacentElement('afterend',box);if(bs){input.value=ad}bind(box,input,p,false)}
function wrapBsText(input){if(input.dataset.osBsWrapped==='1'||input.type!=='text')return;const label=input.closest('label');const txt=label?.querySelector('span')?.textContent||'';if(!/date|birthday|dob/i.test(txt)||!/\bBS\b|Bikram|Nepali/i.test(txt)||/range/i.test(txt))return;style();changeLabel(input);const existing=parts(input.value),fallback=parts(todayBs())||{y:2083,m:1,d:1},p=existing||fallback;input.dataset.osBsWrapped='1';input.classList.add('os-bs-source');const box=document.createElement('div');box.className='os-bs-control';box.innerHTML=picker(p)+'<span class="os-bs-note">Nepali date · BS</span>';input.insertAdjacentElement('afterend',box);bind(box,input,p,true)}
function bind(box,input,p,isBs){const y=box.querySelector('[data-bs-y]'),m=box.querySelector('[data-bs-m]'),d=box.querySelector('[data-bs-d]');const sync=()=>{const yy=+y.value,mm=+m.value,max=days(yy,mm),dd=Math.min(+d.value||1,max);d.innerHTML=Array.from({length:max},(_,i)=>i+1).map(v=>`<option value="${v}" ${v===dd?'selected':''}>${v}</option>`).join('');const bs=`${yy}-${pad(mm)}-${pad(dd)}`;input.dataset.bsValue=bs;input.value=isBs?bs:DC.bsToAd(bs);input.dispatchEvent(new Event('input',{bubbles:true}));input.dispatchEvent(new Event('change',{bubbles:true}))};y.addEventListener('change',sync);m.addEventListener('change',sync);d.addEventListener('change',sync);if(input.value)input.dataset.bsValue=isBs?input.value:((()=>{const p=DC.adToBs(input.value);return p?`${p.y}-${pad(p.m)}-${pad(p.d)}`:''})())}
function scan(root=document){style();root.querySelectorAll?.('input[type="date"]').forEach(wrapNative);root.querySelectorAll?.('input[type="text"]').forEach(wrapBsText)}
scan();new MutationObserver(ms=>ms.forEach(m=>m.addedNodes.forEach(n=>{if(n.nodeType===1)scan(n)}))).observe(document.body,{subtree:true,childList:true});
window.RachnaBS={months:MONTHS,bsToAd:DC.bsToAd,adToBs:DC.adToBs,todayBs};
})();
